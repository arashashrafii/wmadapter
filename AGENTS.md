# دستورالعمل محیط‌های اجرا

## تفکیک محصول و تست

- محیط محصول با `config.yaml` اجرا می‌شود و باید روی پورت `11555` باشد.
- محیط تست با `config.test.yaml` اجرا می‌شود و باید روی پورت `11556` باشد.
- هر عامل برای اجرای تست یا بررسی تغییرات باید از تنظیمات تست استفاده کند:

  ```bash
  .venv/bin/wmadapter --config config.test.yaml
  ```

  یا:

  ```bash
  npm run start:test
  ```

- آدرس پایه API در تست: `http://127.0.0.1:11556/v1`
- آدرس پایه API محصول: `http://127.0.0.1:11555/v1`
- تست‌ها نباید `config.yaml`، پروفایل مرورگر محصول یا لاگ محصول را تغییر دهند.
- قبل از اجرای تست، اگر سرویس تست فعال نیست، فقط سرویس تست را روی پورت `11556` اجرا کنید.
- اجرای محصول برای تست مجاز نیست؛ محصول فقط برای بررسی نهایی یا مصرف واقعی استفاده می‌شود.

## فرمان‌های استاندارد

```bash
# تست‌های خودکار
.venv/bin/pytest -q

# اجرای محصول
npm run start:product

# اجرای محیط تست
npm run start:test
```
